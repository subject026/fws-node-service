const origins = require("./origins");

const parseFormData = (body, origin) => {
  // what data is expected for this origin?
  throw new Error("Ooooof");
  return null;
};

module.exports = parseFormData;
